# Configuracion para el acceso a la Base de Datos
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Datos de conexion con la base de datos MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/simagrow
spring.datasource.username=root
spring.datasource.password=

# Añadir datos de prueba
# (Descomentar y volver a comentar porque en cada modificacion de cualquier
# (archivo, volveria hacer los inserts por la libreria DevTools)

#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always

#spring.jpa.properties.hibernate.dialect =org.hibernate.dialect.MySQL8Dialect